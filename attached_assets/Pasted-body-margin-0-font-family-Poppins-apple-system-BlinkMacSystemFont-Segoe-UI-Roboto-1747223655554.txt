body {
  margin: 0;
  font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
    'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
    sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  background-color: #f8f9fa;
  color: #212529;
}

code {
  font-family: source-code-pro, Menlo, Monaco, Consolas, 'Courier New',
    monospace;
}

/* Importación de Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');

:root {
  --primary-color: #3b71ca;
  --primary-hover: #2c58a0;
  --secondary-color: #6c757d;
  --success-color: #14a44d;
  --danger-color: #dc4c64;
  --warning-color: #e4a11b;
  --info-color: #54b4d3;
  --dark-color: #14141f;
  --light-color: #f8f9fa;
  --body-bg: #f8f9fa;
  --card-bg: #ffffff;
}

/* Mejoras para Bootstrap */
.btn-primary {
  background-color: var(--primary-color);
  border-color: var(--primary-color);
  box-shadow: 0 4px 9px -4px rgba(59, 113, 202, 0.3);
  transition: all 0.2s ease;
}

.btn-primary:hover {
  background-color: var(--primary-hover);
  border-color: var(--primary-hover);
  transform: translateY(-1px);
}

.btn-warning {
  background-color: #FFC107;
  border-color: #FFC107;
  color: #212529;
  font-weight: 600;
  box-shadow: 0 4px 9px -4px rgba(255, 193, 7, 0.3);
  transition: all 0.2s ease;
}

.btn-warning:hover {
  background-color: #e0a800;
  border-color: #e0a800;
  transform: translateY(-1px);
}

.card {
  border-radius: 10px;
  border: none;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
  transition: all 0.3s ease;
}

.card:hover {
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
  transform: translateY(-3px);
}

.nav-link {
  font-weight: 500;
  transition: all 0.2s ease;
}

.nav-link:hover {
  color: var(--primary-color) !important;
}

/* Estilos para la búsqueda de vehículos */
.search-section .card {
  transition: transform 0.2s ease;
}

.search-section .card:hover {
  transform: translateY(-2px);
}

.search-input {
  border-radius: 8px;
  font-size: 1rem;
  padding: 12px 15px;
  border: 1px solid #ced4da;
}

.search-input:focus {
  border-color: var(--primary-color);
  box-shadow: 0 0 0 0.25rem rgba(59, 113, 202, 0.25);
}

.site-checkboxes .form-check-input:checked {
  background-color: var(--primary-color);
  border-color: var(--primary-color);
}

.popular-searches .btn-outline-secondary {
  border-radius: 8px;
  transition: all 0.2s ease;
  border: 1px solid #dee2e6;
}

.popular-searches .btn-outline-secondary:hover {
  background-color: #f8f9fa;
  border-color: var(--primary-color);
  color: var(--primary-color);
}

.popular-searches .btn-outline-secondary .badge {
  transition: all 0.2s ease;
}

.popular-searches .btn-outline-secondary:hover .badge {
  background-color: var(--primary-color);
}

/* Utilidades personalizadas */
.bg-gradient-primary {
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
}

.text-gradient {
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-hover) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.shadow-soft {
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
}

.rounded-xl {
  border-radius: 1rem;
}

.fw-medium {
  font-weight: 500;
}

.section-padding {
  padding: 100px 0;
}

/* Diseño responsivo mejorado */
@media (max-width: 768px) {
  .section-padding {
    padding: 60px 0;
  }
  
  .search-section .site-checkboxes {
    flex-direction: column;
  }
  
  .search-section .site-checkboxes .form-check {
    margin-bottom: 8px;
  }
}

/* Animaciones */
.fade-in {
  animation: fadeIn 0.6s ease-in;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
